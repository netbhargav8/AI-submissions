# Customer Sales Analysis Pipeline

A Python-based data analytics and preprocessing engine designed to clean transactional sales logs, engineer transactional financial fields, compute category/customer metrics, and visualize time-series growth trends.

## Prerequisites & Installation

Ensure you have a Python environment installed. Run the following command in your terminal to install all required dependencies:

```bash
pip install pandas numpy matplotlib scikit-learn