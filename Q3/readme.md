# Simple Iris Classification Pipeline

A supervised machine learning classification pipeline that utilizes a Decision Tree algorithm to recognize and categorize three distinct species of Iris flowers based on physical petal and sepal measurements.

## Prerequisites & Installation

This project requires Python 3 along with Scikit-Learn, NumPy, and Pandas. Install the environment libraries using this terminal command:

```bash
pip install scikit-learn numpy pandas