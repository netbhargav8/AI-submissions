# Application Log File Analyzer

A Python-based log processing engine designed to parse unstructured application log files, isolate critical diagnostic indicators, track occurrences across specific functional modules, and export the findings into a data-driven summary.

## Prerequisites & Installation

This project requires Python 3 and the Pandas data library. You can install the required dependency by running the following command in your terminal:

```bash
pip install pandas