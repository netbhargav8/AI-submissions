# Titanic Survival Prediction Engine

A predictive machine learning pipeline that cleans passenger demographics, performs feature engineering, and trains competing classification algorithms to predict survival outcomes.

## Prerequisites & Installation

This pipeline runs on Python 3 and requires Scikit-Learn, Pandas, NumPy, and Matplotlib. Install the environment stack using your terminal:

```bash
pip install pandas numpy scikit-learn matplotlib